const initialState = {

}

function pinjamanReducer(state = initialState, action) {
    switch (action.type) {
        case 'value':

            break;

        default:
            break;
    }
}

export default pinjamanReducer