const initialState = {

}

function landerReducer(state = initialState, action) {
    switch (action.type) {
        case 'value':

            break;

        default:
            break;
    }
}

export default landerReducer